import { HeroSection, ServicesSection, NewsletterSection, CTASection } from '@/components/home'

export default function HomePage() {
  return (
    <main>
      <HeroSection />
      <ServicesSection />
      <NewsletterSection />
      <CTASection />
    </main>
  )
}